# ==============================================
# Course Platform Backend - Environment Variables
# ==============================================
# Copy this file to .env and fill in your values
# cp .env.example .env
# ==============================================

# Application
NODE_ENV=production
PORT=4000

# Database (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:your_password@host.docker.internal:5432/course_platform

# Google OAuth 2.0
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=https://your-domain.com/auth/google/callback

# JWT Secrets (minimum 32 characters each - generate secure random strings)
# Generate with: openssl rand -base64 48
JWT_SECRET=your_jwt_secret_at_least_32_characters_long_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_at_least_32_chars

# JWT Token Lifetimes (optional - defaults shown)
JWT_ACCESS_TOKEN_TTL_SECONDS=900
JWT_REFRESH_TOKEN_TTL_DAYS=30

# Frontend URLs (comma-separated for multiple origins)
FRONTEND_APP_URLS=https://your-frontend-domain.com

# OAuth State Cookie (optional - defaults shown)
GOOGLE_STATE_COOKIE_NAME=cp_oauth_state
GOOGLE_STATE_MAX_AGE_SECONDS=600

# OpenAI API
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here

# LLM Configuration (optional - defaults shown)
LLM_MODEL=gpt-3.5-turbo
EMBEDDING_MODEL=text-embedding-3-small